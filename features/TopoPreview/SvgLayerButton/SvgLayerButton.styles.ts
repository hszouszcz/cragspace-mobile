import { palette } from '@/src/theme';
import { StyleSheet } from 'react-native';

export const svgLayerButtonStyles = StyleSheet.create({
  button: {
    backgroundColor: palette.blackAlpha55,
  },
});
