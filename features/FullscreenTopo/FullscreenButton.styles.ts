import { palette } from '@/src/theme';
import { StyleSheet } from 'react-native';

export const fullscreenButtonStyles = StyleSheet.create({
  button: {
    backgroundColor: palette.blackAlpha55,
  },
});
